interface:
  display_name: "Prettify PDF"
  short_description: "Redesign PDFs without breaking critical facts"
  default_prompt: "Use $prettify-pdf to redesign this PDF while preserving IDs, dates, times, amounts, barcodes, QR codes, seals, signatures, labels, and source meaning."
